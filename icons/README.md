App art for the screenshots, not for the site itself.

Taken from [dashboard-icons](https://github.com/homarr-labs/dashboard-icons)
(MIT). Each mark stays the trademark of whoever owns it; they appear here for
the same reason they appear on a real desktop — `screenshots.mjs` paints them
onto the sample icons so the mock reads like a machine somebody uses.
